# EXP-03 pinned environment -- mirrors the home box (MeowstickPC) conda env WSTS_original,
# Python 3.10.4, so cross-machine arm comparisons are not confounded by library versions.
#
# Install with the cu128 index; torch 2.7.0+cu128 is the build that carries sm_120 kernels
# (Blackwell: home's RTX 5070 and miles-workspace's RTX 5090 are both sm_120).
#   pip install -r requirements.lock.txt --extra-index-url https://download.pytorch.org/whl/cu128
#
# DELIBERATELY OMITTED from the home freeze (not on the training path, ~90 packages):
#   - the MLflow stack (mlflow, databricks-sdk, docker, Flask, fastapi, uvicorn, gunicorn,
#     alembic, SQLAlchemy, graphene, opentelemetry-*) -- FE_WSTS tracks with wandb, not MLflow;
#     that stack belongs to old_repo_FE_WSTS.
#   - the Jupyter stack (jupyterlab, notebook, ipykernel, nbclassic, y-py) -- not used to train.
#   - nvidia-*-cu11 wheels -- residue from an earlier torch 2.0.0 install, dead under cu128.
# None of these affect numerics. Add them back only if you need the notebooks.

# --- core numerics (version-sensitive: pin exactly) ---
torch==2.7.0+cu128
torchvision==0.22.0+cu128
triton==3.3.0
numpy==1.26.4
scipy==1.11.4
pandas==1.4.2
h5py==3.7.0

# --- training framework ---
pytorch-lightning==2.0.1
lightning-utilities==0.15.3
torchmetrics==1.4.0
jsonargparse[signatures]==4.20.1

# --- model zoo ---
segmentation-models-pytorch==0.3.2
timm==0.6.12
efficientnet_pytorch==0.7.1
pretrainedmodels==0.7.4
einops==0.6.1

# --- data / geo / imaging ---
# headless build: identical OpenCV 4.7.0.72 without the GUI backends. The GUI build needs
# libGL.so.1, absent on headless boxes (miles-workspace has no sudo to add it). cv2 is not
# imported anywhere in src/ or by segmentation_models_pytorch, so this cannot affect numerics.
opencv-python-headless==4.7.0.72
scikit-learn==1.2.2
rasterio==1.3.6
geopandas==0.12.2
fiona==1.10.1
shapely==2.1.2
pyproj==3.7.1
xarray==2023.3.0
imageio==2.27.0
matplotlib==3.7.1
pillow==12.3.0

# --- tracking ---
wandb==0.15.3
protobuf==4.25.9

# --- misc runtime ---
PyYAML==6.0.3
tqdm==4.65.0
setuptools==70.0.0
